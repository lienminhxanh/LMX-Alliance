---
description: Run all tests và check TypeScript errors
allowed-tools: Bash(npm run*), Bash(npx *)
---

# Run Tests

Execute the following:
1. Run `npm run type-check` to verify TypeScript
2. Run `npm run lint` to check ESLint
3. Run `npm run test` to run unit tests
4. Report any errors found